
enum estado_planilla_enum {
  abierto
  en_revision
  congelado
  declarado
}

enum estado_tarea_enum {
  pendiente
  revision
  aprobado
}

enum rol_usuario_enum {
  ADMIN
  CONTADOR
  RRHH
  ASISTENTE
  EMPLEADO
}
